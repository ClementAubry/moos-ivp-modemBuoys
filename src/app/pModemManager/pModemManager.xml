
<?xml version="1.0" encoding="UTF-8"?>
<!-- This file contains pModemManager documentation -->
<?xml-stylesheet href="../../../doc/xmldocumentation.css" type="text/css"?>
<xmldoc>
  <moosapp>

     <info>
        <name>pModemManager</name>
        <organization>ENSTA Bretagne</organization>
        <date>2015</date>
        <authors>
           <author>Clément Aubry</author>
        </authors>
        <licence>MIT</licence>
     </info>

     <documentation>
        <synopsis>
          This App hold modem powering up process with an without magnet, in order to boot them up in config or in communication mode.
          When a modem configuration is required (scheduled), we follow this procedure :
            * Power down the modem,
            * Power up the electromagnet,
            * Power up the modem,
            * Publish MODEM_CONFIGURATION_REQUIRED="master" or "slave" depending the conf we need,
            * When receiving "MODEM_IS_ALIVE" = true, power down the magnet,
            * When receiving "MODEM_CONFIGURATION_COMPLETE" = true, reboot the modem by powering it down and up.
        </synopsis>
        <interface>
           <subscriptions>
              <moosvar>
                 <varname>MODEM_IS_ALIVE</varname>
                 <vartype>bool</vartype>
                 <varinfo>A boolean setted to true when iModem receive some mtAlive messages, means that the magnet can be powered off.</varinfo>
              </moosvar>
              <moosvar>
                 <varname>MODEM_CONFIGURATION_COMPLETE</varname>
                 <vartype>bool</vartype>
                 <varinfo>A boolean setted to true by iModem when the configuration process is complete.</varinfo>
              </moosvar>
           </subscriptions>
           <publications>
              <moosvar>
                 <varname>MODEM_CONFIGURATION_REQUIRED</varname>
                 <vartype>string</vartype>
                 <varinfo>A string that signal to iModem that we need to change modem configuration. The role asked has to be contained in that string ("master" or "slave")</varinfo>
              </moosvar>
           </publications>
        </interface>
        <!-- if there is a bug or a special fact to know.. -->
        <optional-comments>
           No comment.
        </optional-comments>
        <!-- if something can be improved -->
        <suggested-improvements>
           No suggested improvements.
        </suggested-improvements>
     </documentation>

  </moosapp>
</xmldoc>
